{
  "name": "jobs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "trigger dev",
    "deploy": "trigger deploy"
  },
  "dependencies": {
    "trigger.dev": "^4.1.2",
    "@trigger.dev/sdk": "^3.0.0"
  }
}
